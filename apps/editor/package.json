{
  "name": "@srb/editor",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'editor dev — TODO'",
    "build": "echo 'editor build — TODO'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
