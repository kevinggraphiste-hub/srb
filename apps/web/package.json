{
  "name": "@srb/web",
  "version": "0.5.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'web dev — TODO'",
    "build": "echo 'web build — TODO'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .next .turbo"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
