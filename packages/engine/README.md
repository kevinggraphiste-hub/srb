# @srb/engine

Logique moteur partagée entre l'éditeur et le runtime : map, collisions, interpréteur d'events, formules de combat, DB schemas.

Pure TypeScript, aucune dépendance framework. Utilisé par `@srb/editor` (pour l'aperçu) et `@srb/player` (pour l'exécution réelle).
