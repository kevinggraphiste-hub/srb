# @srb/schemas

Schémas Zod de validation runtime partagés entre client et serveur. Utilisés pour valider les inputs de l'API, les JSON de projet chargés, les messages WebSocket, etc.

Import `@srb/schemas` partout où la donnée traverse une frontière de confiance.
